meta { name: List categories; type: http; seq: 1 }
get { url: {{baseUrl}}/categories }
tests { test("Categories return HTTP 200", function() { expect(res.status).to.equal(200); expect(res.body.length).to.be.greaterThan(0); }); }
