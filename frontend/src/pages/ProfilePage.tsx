import { Link } from 'react-router-dom'; import { useAuth } from '../state/AuthContext';
export function ProfilePage(){const {user}=useAuth();if(!user)return <section className="card"><h1>Mi perfil</h1><p>Ingresa para consultar tu perfil.</p><Link className="button" to="/login">Ingresar</Link></section>;return <section className="card narrow"><h1>Mi perfil</h1><p><strong>Nombre:</strong> {user.name}</p><p><strong>Correo:</strong> {user.email}</p><p><strong>Roles:</strong> {user.roles.join(', ')}</p><p className="success">Tu sesión está protegida con JWT. Las credenciales nunca se muestran.</p></section>}
